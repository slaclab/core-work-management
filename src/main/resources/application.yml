logging:
  level:
    edu.stanford.slac.core_work_management: DEBUG
    org.springdoc: debug

server:
  tomcat:
    mbeanregistry:
      enabled: true

edu:
  stanford:
    slac:
      core_work_management:

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  metrics:
    tags:
      application: ${spring.application.name}

spring:
  application:
    name: 'core-work-management' 

# swagger-ui custom path
springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api-docs